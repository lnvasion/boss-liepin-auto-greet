# Phase 0: BOSS直聘推荐页 DOM 结构发现文档

> **状态**: 待完成 — 需要用户在真实浏览器中运行 `scout.user.js` 收集数据
>
> **目标页面**: `https://www.zhipin.com/web/chat/recommend`

## 操作步骤

1. 打开 Chrome，确保已安装 **Tampermonkey** 扩展
2. 登录 BOSS 直聘网站
3. 在 Tampermonkey 中新建脚本，粘贴 `scout.user.js` 的内容，保存
4. 导航到 `https://www.zhipin.com/web/chat/recommend`
5. 打开 DevTools (F12) → Console 标签
6. 观察控制台输出的侦察报告
7. 将以下信息填入本文档对应位置

---

## 1. 页面基本信息

| 项目 | 值 |
|------|-----|
| URL | (填写) |
| 页面标题 | (填写) |
| 框架 | (Vue 2 / Vue 3 / React / 其他) |
| 框架版本 | (填写) |

## 2. 推荐卡片选择器

| 优先级 | CSS 选择器 | 匹配数量 | 备注 |
|--------|-----------|---------|------|
| 1 | (从侦察脚本输出获取) | | |
| 2 | | | |
| 3 | | | |

## 3. "立即沟通"按钮选择器

| 优先级 | CSS 选择器 | 按钮文本 |
|--------|-----------|---------|
| 1 | (从侦察脚本输出获取) | |
| 2 | | |

## 4. 沟通弹窗选择器

| 项目 | CSS 选择器 |
|------|-----------|
| 弹窗容器 | (填写) |
| 招呼语输入框 | (填写) |
| 发送按钮 | (填写) |

## 5. Vue 组件实例信息

| 项目 | 值 |
|------|-----|
| Vue 实例访问路径 | `__vue__` / `__vue_app__` / 其他 |
| 沟通触发方法名 | (填写) |
| 弹窗显示状态键 | (填写) |
| 关键数据键 | (填写) |

## 6. WebSocket 通信

| 项目 | 值 |
|------|-----|
| WebSocket URL | (填写) |
| 协议格式 | JSON / Protobuf / 其他 |
| 发送消息格式样例 | (填写) |

## 7. Cookie / 认证

| Cookie 名称 | 说明 |
|-------------|------|
| (从侦察脚本获取) | |

## 8. 其他发现

- 是否有字体反爬？
- 是否有 `debugger` 反调试？
- 是否有 CSP 限制？
- 验证码类型？

---

## 侦察脚本输出截图

(粘贴控制台截图或用文本记录关键输出)
